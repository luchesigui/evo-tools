{
  "name": "evo-kommo-broadcast",
  "version": "1.0.0",
  "description": "Envio de mensagens em massa via WhatsApp pelo Kommo CRM",
  "private": true,
  "scripts": {
    "install": "python3 -m venv venv && ./venv/bin/pip install -r requirements.txt",
    "start": "./venv/bin/python broadcast.py",
    "clean": "rm -rf __pycache__ .pytest_cache venv"
  }
}